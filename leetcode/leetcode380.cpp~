#include<bits/stdc++.h>
using namespace std;

void printV(vector<int> v)
{
  for(auto i:v){
    cout<<i<<' ';
  }
  cout<<'\n';
}

void printM(vector<vector<int>> m)
{
  for(auto v:m){
    printV(v);
  }
}

class RandomizedSet {
public:
  unordered_set<int> rset;
  RandomizedSet() {
        
  }
    
  bool insert(int val) {
    if(rset.count(val))
      return true;

    else
      rset.insert(val);
    
    return false;
  }
    
  bool remove(int val) {
    
  }
    
  int getRandom() {
    
  }
};

int main()
{
  return 0;
}
