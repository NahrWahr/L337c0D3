#include<bits/stdc++.h>
using namespace std;

bool check(vector<vector<int>> &h, int i, int j)
{
  
}

vector<vector<int>> pacificAtlantic(vector<vector<int>> &h)
{
  int m=h.size();
  int n=h[0].size();
  vector<vector<int>> res;

  for(int i=0;i<m;i++){
    for(int j=0;j<n;j++){
      h[i][j];
    }
  }
  return res;
}

int main()
{
  return 0;
}
