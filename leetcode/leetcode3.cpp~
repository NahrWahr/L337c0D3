#include <bits/stdc++.h>
using namespace std;

int lengthOfLongestSubstring(string s)
{
  int lm=0, gm=0;
  int l=s.length();
  for(int i=0;i<l;i++){
    lm = max(lm, lm+1);
    gm = max(gm, lm);
  }
  return gm;
}
  
int main(){
  return 0;
}
