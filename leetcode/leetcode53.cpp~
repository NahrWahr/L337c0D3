#include<bits/stdc++.h>
using namespace std;

int maxSubArray(vector<int> &x)
{
  int n=x.size();
  int m=INT_MIN;

  for(int i=0; i<n; i++){
  }
  
  return m;
}

int main()
{
  vector<int> x = {1,2,3,23,4,2,21,2,4,3,21,34};
  cout<<maxSubArray(x);
  std::max(7,8);
  return 0;
}
