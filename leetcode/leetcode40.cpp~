#include<bits/stdc++.h>
using namespace std;

void printV(vector<int> v){
  for(int i:v){
    cout<<i<<' ';
  }
  cout<<'\n';
}

void printM(vector<vector<int>> m){
  for(vector<int> v:m){
    printV(v);
  }
}

vector<vector<int>> combinationSum2(vector<int>& x, int t)
{
  int n = x.size();
  vector<vector<int> res;
  
  return res;
}

int main()
{
  return 0;
}
