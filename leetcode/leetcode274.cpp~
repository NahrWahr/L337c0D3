#include<bits/stdc++.h>
using namespace std;

int hIndex(vecor<int> &v)
{
  int h=0;
  int n=v.size();
  sort(v.begin(), v.end());
  h=v[n/2];
  return h;
}

int main()
{
  uint32_t x=1024;
  cout<<hammingWeight(x);
  return 0;
}
