#include<bits/stdc++.h>
using namespace std;

void printV(vector<int> v){
  for(int i:v){
    cout<<i<<' ';
  }
  cout<<'\n';
}

void printM(vector<vector<int>> m){
  for(vector<int> v:m){
    printV(v);
  }
}

vector<string> res;
int n;

string helper(string s, int dots, int index, string tmp)
{
  if(dots==3 && index == n)
    return tmp;
}

vector<string> restoreIpAddresses(string s)
{
  n = s.length();
}

int main()
{
  return 0;
}
