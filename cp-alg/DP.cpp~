#include<bits/stdc++.h>
using namespace std;

void printV(vector<int> v){
  for(int i:v){
    cout<<i<<' ';
  }
  cout<<'\n';
}

void printM(vector<vector<int>> m){
  for(vector<int> v:m){
    printV(v);
  }
}

int fib(int n)
{
  if(n==1 || n==2)
    return 1;
  return fib(n-1) + fib(n-2);
}

int main()
{
  
  return 0;
}
